import React, { useState } from 'react';
import { Route, Routes } from 'react-router-dom';

export default () => {
    return (
        <div>
            this is the better version of jobly
        </div>
    )
}